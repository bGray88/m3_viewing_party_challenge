<%= button_to "Create New User", register_path, method: :get %>
<section class='existing-users'>
    <% if current_user %>
      <p>Existing Users: </p>
      <% @users.each do |user| %>
          <li><%= "#{user.email}" %></li>
      <% end %>
    <% end %>
</section>