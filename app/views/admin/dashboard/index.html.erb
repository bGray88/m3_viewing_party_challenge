<h1>Admin Dashboard</h1>

<section class='existing-users'>
<p>All Users: </p>
  <% @users.each do |user| %>
    <li><%= link_to user.email, admin_user_dashboard_path(user) %></li>
  <% end %>
</section>
